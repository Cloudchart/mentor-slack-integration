extends ../layout

block content
  main(
    data-react-class="TeamUsersApp"
  )

block pagescript
  script(type="text/javascript").
    window.__INITIAL_STATE__ = {
      viewedTeam: !{JSON.stringify(viewedTeam)},
      team: !{JSON.stringify(team)},
      users: !{JSON.stringify(users)}
    }
